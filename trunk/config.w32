// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("lnice", "for lnice support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("lnice", "enable lnice support", "no");

if (PHP_LNICE != "no") {
	EXTENSION("lnice", "lnice.c");
}

